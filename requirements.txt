# Pinned environment in which the shipped reference scores were verified to
# reproduce. For ordinary use install the package instead, which takes looser
# bounds from pyproject.toml:
#
#     pip install -e ".[data,baselines]"

numpy==2.5.1
pandas==3.0.5
huggingface_hub==1.24.0

# reference baselines (causaldemand.baselines)
statsmodels==0.14.6
scikit-learn==1.9.0

# notebooks
matplotlib==3.11.1

# tests
pytest==9.1.1
